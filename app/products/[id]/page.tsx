export async function generateStaticParams() {
  // Return an empty array since we don't have dynamic params to pre-render
  return [];
}

export default function ProductPage() {
  return (
    <div>
      <h1>Product Page</h1>
    </div>
  );
}